#include<bits/stdc++.h>
using namespace std;
int main()
{
    int time,velocity;
    while(scanf("%d%d",&velocity,&time)!=EOF)
    {
        printf("%d\n",2*velocity*time);
    }
    return 0;
}
